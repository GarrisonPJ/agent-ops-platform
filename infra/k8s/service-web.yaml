apiVersion: v1
kind: Service
metadata:
  name: agentops-web
  namespace: agentops
  labels:
    app: agentops
    component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: agentops
    component: web
