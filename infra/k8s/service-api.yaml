apiVersion: v1
kind: Service
metadata:
  name: agentops-api
  namespace: agentops
  labels:
    app: agentops
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: agentops
    component: api
