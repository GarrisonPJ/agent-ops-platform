[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    smoke: E2E tests that require a running PostgreSQL instance (docker compose)
    needs_postgresql: tests that require a PostgreSQL database (skipped in CI)
