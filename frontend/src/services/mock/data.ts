import type {
  TrajectorySummary,
  TrajectoryDetail,
  ToolInfo,
  BenchmarkTask,
  CompareResponse,
  FailureSummary,
  FailureReport,
  PolicyVersion,
  WarmupStatus,
} from "../../types";

/* ─── Traces (TrajectorySummary) ────────────────────────────────── */

export const mockTrajectories: TrajectorySummary[] = [
  {
    id: "tr-a1b2c3d4",
    task: "Analyze production logs and isolate the latency spike in the payment service",
    status: "success",
    step_count: 8,
    created_at: new Date(Date.now() - 3_600_000).toISOString(),
    score: 0.92,
  },
  {
    id: "tr-e5f6g7h8",
    task: "Debug the recurring API timeout on /v2/checkout endpoint",
    status: "failed",
    step_count: 6,
    created_at: new Date(Date.now() - 86_400_000).toISOString(),
    score: 0.45,
  },
  {
    id: "tr-i9j0k1l2",
    task: "Refactor legacy SQL queries for the reporting dashboard",
    status: "success",
    step_count: 10,
    created_at: new Date(Date.now() - 172_800_000).toISOString(),
    score: 0.88,
  },
  {
    id: "tr-m3n4o5p6",
    task: "Investigate memory leak in stream processing pipeline",
    status: "failed",
    step_count: 5,
    created_at: new Date(Date.now() - 259_200_000).toISOString(),
    score: 0.32,
  },
  {
    id: "tr-q7r8s9t0",
    task: "Audit dependency security vulnerabilities for Q3 release",
    status: "running",
    step_count: 4,
    created_at: new Date(Date.now() - 600_000).toISOString(),
    score: null,
  },
  {
    id: "tr-u1v2w3x4",
    task: "Optimize Redis cache invalidation strategy for user sessions",
    status: "success",
    step_count: 7,
    created_at: new Date(Date.now() - 345_600_000).toISOString(),
    score: 0.79,
  },
  {
    id: "tr-y5z6a7b8",
    task: "Generate unit test suite for the billing module",
    status: "success",
    step_count: 12,
    created_at: new Date(Date.now() - 518_400_000).toISOString(),
    score: 0.95,
  },
  {
    id: "tr-c9d0e1f2",
    task: "Trace cross-service transaction failures in the order flow",
    status: "success",
    step_count: 9,
    created_at: new Date(Date.now() - 604_800_000).toISOString(),
    score: 0.71,
  },
];

/* ─── Trace Details ────────────────────────────────────────────── */

export const mockTraceDetails: Record<string, TrajectoryDetail> = {
  "tr-a1b2c3d4": {
    id: "tr-a1b2c3d4",
    task: "Analyze production logs and isolate the latency spike in the payment service",
    steps: [
      {
        index: 0,
        thought: "I'll start by fetching the recent production logs for the payment service from the last 4 hours.",
        action: { id: "read_logs", name: "read_file", arguments: { path: "/var/log/payment-service/error.log", lines: 500 } },
        observation: "Returned 487 lines. Found 23 ERROR entries, 142 WARN entries. Latency p95 = 3400ms (threshold: 800ms).",
        latency_ms: 1200,
        started_at: Date.now() - 3600000,
        context_window: { used: 3200, limit: 128000 },
        token_prompt: 1500,
        token_completion: 280,
      },
      {
        index: 1,
        thought: "The p95 latency is 4x above threshold. Let me check the upstream database query times — the payment service heavily depends on PostgreSQL.",
        action: { id: "run_query", name: "execute_command", arguments: { command: "psql -c \"SELECT queryid, calls, mean_time_ms FROM pg_stat_statements ORDER BY mean_time_ms DESC LIMIT 10;\"" } },
        observation: "Top query: queryid=847291, calls=14230, mean_time_ms=287. Query pattern: SELECT * FROM payment_transactions WHERE user_id = $1 AND created_at > $2 — no index on created_at.",
        latency_ms: 3400,
        started_at: Date.now() - 3590000,
        context_window: { used: 5800, limit: 128000 },
        token_prompt: 2100,
        token_completion: 450,
      },
      {
        index: 2,
        thought: "Found the root cause. The payment_transactions table is missing a composite index on (user_id, created_at). The sequential scan is causing the high latency. Let me also check if there's a Redis cache layer.",
        action: { id: "check_cache", name: "execute_command", arguments: { command: "redis-cli -h cache-01 --scan --pattern 'payment:*' | head -5" } },
        observation: "Redis cache miss rate: 68%. Cache keys follow pattern 'payment:user:{userId}'. Only 32% of payment lookups are cached.",
        latency_ms: 800,
        started_at: Date.now() - 3580000,
        context_window: { used: 8200, limit: 128000 },
        token_prompt: 1800,
        token_completion: 320,
      },
      {
        index: 3,
        thought: "Two issues compounding each other: missing DB index + low cache hit rate. Let me verify the index recommendation by checking the schema.",
        action: { id: "get_schema", name: "read_file", arguments: { path: "migrations/2024_schema.sql", lines: 200 } },
        observation: "Found CREATE TABLE payment_transactions definition. Indexes present: PRIMARY KEY (id), INDEX (user_id). Missing: composite index (user_id, created_at).",
        latency_ms: 600,
        started_at: Date.now() - 3575000,
        context_window: { used: 10500, limit: 128000 },
        token_prompt: 900,
        token_completion: 180,
      },
      {
        index: 4,
        thought: "Confirmed. Now let me check if there's any ongoing deployment that might have caused a regression.",
        action: { id: "check_deploy", name: "execute_command", arguments: { command: "kubectl get deployments -n payment --sort-by=.metadata.creationTimestamp | tail -3" } },
        observation: "Last deployment: 'payment-api' version v2.14.3 rolled out 6 hours ago. Rollout history shows a change in connection pool settings.",
        latency_ms: 1500,
        started_at: Date.now() - 3565000,
        context_window: { used: 13000, limit: 128000 },
        token_prompt: 1200,
        token_completion: 210,
      },
      {
        index: 5,
        thought: "The deployment changed max_connections from 20 to 50 and added a new query pattern. Combined with the missing index, this amplified the latency. Let me generate the fix.",
        action: { id: "propose_fix", name: "write_file", arguments: { path: "fixes/20240704_add_index.sql", content: "CREATE INDEX CONCURRENTLY idx_payment_txns_user_created ON payment_transactions (user_id, created_at);\n-- Estimated impact: reduce p95 from 3400ms to ~200ms" } },
        observation: "Fix file written. Next steps: review migration, apply during maintenance window, monitor p95.",
        latency_ms: 400,
        started_at: Date.now() - 3550000,
        context_window: { used: 16000, limit: 128000 },
        token_prompt: 650,
        token_completion: 150,
      },
      {
        index: 6,
        thought: "Let me also suggest a cache warmup strategy to complement the index fix. For the payment service, we should cache the last 10 transactions per user proactively.",
        action: { id: "cache_plan", name: "write_file", arguments: { path: "fixes/cache_warmup.md", content: "Strategy:\n1. After DB index is applied, add Redis cache-aside pattern\n2. Set TTL to 300s for payment lookups\n3. Pre-warm cache for top 1000 users by transaction volume" } },
        observation: "Cache warmup plan documented alongside the SQL fix.",
        latency_ms: 500,
        started_at: Date.now() - 3530000,
        context_window: { used: 19200, limit: 128000 },
        token_prompt: 700,
        token_completion: 160,
      },
      {
        index: 7,
        thought: "All findings are documented. The main issues are: 1) Missing composite index (user_id, created_at) causing full table scans, 2) Low Redis cache hit rate at 32%, 3) Recent deployment increased connection pool size amplifying the impact.",
        action: null,
        observation: "Analysis complete. 3 issues found with remediation plan. Estimated p95 improvement: 3400ms → <200ms.",
        latency_ms: 200,
        started_at: Date.now() - 3500000,
        context_window: { used: 22000, limit: 128000 },
        token_prompt: 400,
        token_completion: 200,
      },
    ],
    status: "success",
    created_at: new Date(Date.now() - 3_600_000).toISOString(),
    total_tokens: 14250,
    context_window_peak: 22000,
    score: 0.92,
    score_breakdown: { accuracy: 0.95, efficiency: 0.88, completeness: 0.93 },
  },
  "tr-e5f6g7h8": {
    id: "tr-e5f6g7h8",
    task: "Debug the recurring API timeout on /v2/checkout endpoint",
    steps: [
      {
        index: 0,
        thought: "Let me check the API gateway logs for the /v2/checkout endpoint to understand the timeout pattern.",
        action: { id: "gateway_logs", name: "execute_command", arguments: { command: "kubectl logs -n ingress -l app=gateway --tail=200 | grep '/v2/checkout' | grep 'timeout'" } },
        observation: "Found 47 timeout events in last 5 minutes. 504 responses with upstream_response_time ranging from 28s to 62s (nginx default timeout: 60s).",
        latency_ms: 2000,
        started_at: Date.now() - 86400000,
        context_window: { used: 4100, limit: 128000 },
        token_prompt: 1800,
        token_completion: 300,
      },
      {
        index: 1,
        thought: "The upstream is timing out. Let me trace which internal service handles /v2/checkout and check its health.",
        action: { id: "trace_service", name: "read_file", arguments: { path: "/etc/nginx/conf.d/api-upstreams.conf", lines: 100 } },
        observation: "Found upstream 'checkout-service' directing to backend:8000 with proxy_read_timeout 60s. Service appears to be running but responding slowly.",
        latency_ms: 800,
        started_at: Date.now() - 86380000,
        context_window: { used: 7200, limit: 128000 },
        token_prompt: 1100,
        token_completion: 200,
      },
      {
        index: 2,
        thought: "Let me check the checkout service's recent logs and resource usage.",
        action: { id: "service_health", name: "execute_command", arguments: { command: "kubectl top pod -n backend -l app=checkout-service" } },
        observation: "CPU: 2.8 cores (limit: 4), Memory: 1.2GB / 2GB. Connection pool: 48/50 active connections. No obvious resource exhaustion.",
        latency_ms: 1500,
        started_at: Date.now() - 86350000,
        context_window: { used: 10500, limit: 128000 },
        token_prompt: 1400,
        token_completion: 280,
      },
      {
        index: 3,
        thought: "Not a resource issue. Let me check if there's a deadlock in the checkout flow by examining recent stack traces.",
        action: { id: "stack_trace", name: "execute_command", arguments: { command: "curl -s localhost:8000/debug/pprof/goroutine?debug=2 | grep -A 10 'checkout' | head -60" } },
        observation: "Found 12 goroutines stuck on 'sync.Mutex.Lock' in checkout.go:287 — acquiring payment gateway lock. Payment gateway external API appears to be hanging.",
        latency_ms: 3000,
        started_at: Date.now() - 86320000,
        context_window: { used: 14800, limit: 128000 },
        token_prompt: 1600,
        token_completion: 350,
      },
      {
        index: 4,
        thought: "The payment gateway integration has a blocking mutex without timeout. When the external payment API is slow, it blocks all checkout goroutines. Let me check the code.",
        action: { id: "read_code", name: "read_file", arguments: { path: "services/checkout/checkout.go", start_line: 270, end_line: 310 } },
        observation: "Line 287: paymentGatewayMu.Lock() — no context.WithTimeout or select pattern. The mutex is held until the external HTTP call completes, which can take up to 60s.",
        latency_ms: 500,
        started_at: Date.now() - 86280000,
        context_window: { used: 17200, limit: 128000 },
        token_prompt: 800,
        token_completion: 180,
      },
      {
        index: 5,
        thought: "The fix should replace the blocking mutex with a semaphore + context timeout pattern. However, I don't have write access to this repo for auto-fix.",
        action: { id: "report_findings", name: "write_file", arguments: { path: "fixes/checkout_timeout_findings.md", content: "Root Cause: Blocking mutex at checkout.go:287 without timeout\n\next API call holds lock for up to 60s\nFix: Replace sync.Mutex with channel-based semaphore + context.WithTimeout\n\nPriority: P0 (production outage)\nAffected users: ~15% of checkout attempts timing out\nWorkaround: Temporarily increase nginx proxy_read_timeout to 120s" } },
        observation: "Findings documented. Handoff to engineering team required for code change.",
        latency_ms: 400,
        started_at: Date.now() - 86240000,
        context_window: { used: 20000, limit: 128000 },
        token_prompt: 500,
        token_completion: 200,
      },
    ],
    status: "failed",
    created_at: new Date(Date.now() - 86_400_000).toISOString(),
    total_tokens: 8900,
    context_window_peak: 20000,
    score: 0.45,
    score_breakdown: { accuracy: 0.60, efficiency: 0.40, completeness: 0.35 },
  },
  "tr-i9j0k1l2": {
    id: "tr-i9j0k1l2",
    task: "Refactor legacy SQL queries for the reporting dashboard",
    steps: [
      {
        index: 0,
        thought: "Let me start by reviewing the current reporting dashboard queries to understand the scope of refactoring needed.",
        action: { id: "read_queries", name: "read_file", arguments: { path: "dashboard/reports/queries.sql", lines: 300 } },
        observation: "Found 15 legacy queries. 8 use SELECT *, 5 lack WHERE clause filtering, 3 have N+1 patterns. Average query time: 4.2s.",
        latency_ms: 1200,
        started_at: Date.now() - 172800000,
        context_window: { used: 4500, limit: 128000 },
        token_prompt: 1900,
        token_completion: 260,
      },
      {
        index: 1,
        thought: "The biggest offender is the daily revenue report query doing a full table scan on orders. Let me check the execution plan.",
        action: { id: "explain_query", name: "execute_command", arguments: { command: "psql -c \"EXPLAIN ANALYZE SELECT o.*, u.name FROM orders o JOIN users u ON o.user_id = u.id WHERE o.created_at >= NOW() - INTERVAL '30 days' ORDER BY o.total DESC;\"" } },
        observation: "Seq Scan on orders (cost=0.00..48291.00 rows=142380 width=128), Hash Join on users (cost=1245..18934 rows=142380). Missing index on orders.created_at. Actual time: 6380ms.",
        latency_ms: 4500,
        started_at: Date.now() - 172700000,
        context_window: { used: 8200, limit: 128000 },
        token_prompt: 2500,
        token_completion: 480,
      },
      {
        index: 2,
        thought: "The main performance bottleneck is clear. Let me rewrite each query one by one with proper indexing.",
        action: { id: "create_index", name: "execute_command", arguments: { command: "psql -c \"CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_orders_created_at_total ON orders (created_at DESC, total DESC);\"" } },
        observation: "Index created successfully. Estimated maintenance: 45s during off-peak.",
        latency_ms: 3200,
        started_at: Date.now() - 172500000,
        context_window: { used: 11100, limit: 128000 },
        token_prompt: 1100,
        token_completion: 200,
      },
      {
        index: 3,
        thought: "Now let me rewrite the queries. First, the revenue report — selecting specific columns, adding proper WHERE, and using the new index.",
        action: { id: "rewrite_query", name: "write_file", arguments: { path: "dashboard/reports/queries_optimized.sql", content: "SELECT o.id, o.total, o.currency, o.created_at, u.name\nFROM orders o\nJOIN users u ON o.user_id = u.id\nWHERE o.created_at >= NOW() - INTERVAL '30 days'\n  AND o.status IN ('completed', 'refunded')\nORDER BY o.total DESC;" } },
        observation: "Optimized query written. Added column selection, status filtering, and leverages the new composite index.",
        latency_ms: 600,
        started_at: Date.now() - 172300000,
        context_window: { used: 13800, limit: 128000 },
        token_prompt: 800,
        token_completion: 180,
      },
      {
        index: 4,
        thought: "Let me verify the optimization by re-running the EXPLAIN ANALYZE on the rewritten query.",
        action: { id: "verify", name: "execute_command", arguments: { command: "times=3; for i in $(seq 1 $times); do psql -c \"EXPLAIN ANALYZE SELECT o.id, o.total, o.currency, o.created_at, u.name FROM orders o JOIN users u ON o.user_id = u.id WHERE o.created_at >= NOW() - INTERVAL '30 days' AND o.status IN ('completed', 'refunded') ORDER BY o.total DESC;\"; done" } },
        observation: "Query execution time: 142ms, 138ms, 151ms. Previous: 6380ms. Improvement: ~44x faster. Using Index Scan Backward on idx_orders_created_at_total.",
        latency_ms: 3800,
        started_at: Date.now() - 172000000,
        context_window: { used: 17200, limit: 128000 },
        token_prompt: 1200,
        token_completion: 200,
      },
      {
        index: 5,
        thought: "Excellent — down from 6.38s to ~144ms. Let me continue with the remaining 14 queries following the same pattern.",
        action: null,
        observation: "Refactored 7 more queries: monthly metrics (3.2s → 210ms), top products (5.1s → 180ms), user retention (8.4s → 350ms). Applied CTE pattern for complex aggregations.",
        latency_ms: 10000,
        started_at: Date.now() - 171800000,
        context_window: { used: 22500, limit: 128000 },
        token_prompt: 0,
        token_completion: 0,
      },
      {
        index: 6,
        thought: "Let me check the remaining queries. The user churn analysis query has an anti-pattern with NOT IN on a subquery.",
        action: { id: "fix_churn", name: "write_file", arguments: { path: "dashboard/reports/churn_fix.sql", content: "WITH active_users AS (\n  SELECT DISTINCT user_id FROM orders WHERE created_at >= NOW() - INTERVAL '90 days'\n)\nSELECT u.id, u.name, u.last_login\nFROM users u\nWHERE NOT EXISTS (SELECT 1 FROM active_users au WHERE au.user_id = u.id)\nAND u.role = 'customer';" } },
        observation: "Replaced NOT IN (subquery) with NOT EXISTS anti-join pattern. Estimated improvement: 8.4s → ~350ms.",
        latency_ms: 400,
        started_at: Date.now() - 171500000,
        context_window: { used: 24800, limit: 128000 },
        token_prompt: 700,
        token_completion: 160,
      },
      {
        index: 7,
        thought: "Let me run the complete test suite to make sure all refactored queries return the correct results.",
        action: { id: "run_tests", name: "execute_command", arguments: { command: "psql -f dashboard/tests/query_validation.sql" } },
        observation: "14/15 query tests PASSED. 1 query returning different row count — the refunds report query has a logic difference. Need to review.",
        latency_ms: 2500,
        started_at: Date.now() - 171200000,
        context_window: { used: 27500, limit: 128000 },
        token_prompt: 900,
        token_completion: 180,
      },
      {
        index: 8,
        thought: "The refunds query originally used LEFT JOIN including cancelled transactions. My rewrite used INNER JOIN which excludes them. Let me fix that.",
        action: { id: "fix_refund", name: "edit_file", arguments: { path: "dashboard/reports/queries_optimized.sql", search: "JOIN refunds r ON o.id = r.order_id", replace: "LEFT JOIN refunds r ON o.id = r.order_id" } },
        observation: "Fix applied. Re-ran test: 15/15 PASSED. All query results match the originals.",
        latency_ms: 300,
        started_at: Date.now() - 171000000,
        context_window: { used: 29800, limit: 128000 },
        token_prompt: 500,
        token_completion: 120,
      },
      {
        index: 9,
        thought: "All 15 queries refactored and verified. Average query time reduced from 4.2s to 210ms. Total improvement: ~20x. Let me generate the summary report.",
        action: null,
        observation: "Refactoring complete. 15 queries optimized. Average query time: 4.2s → 210ms (20x improvement). Created new index idx_orders_created_at_total. All tests passing.",
        latency_ms: 300,
        started_at: Date.now() - 170800000,
        context_window: { used: 32000, limit: 128000 },
        token_prompt: 400,
        token_completion: 190,
      },
    ],
    status: "success",
    created_at: new Date(Date.now() - 172_800_000).toISOString(),
    total_tokens: 18500,
    context_window_peak: 32000,
    score: 0.88,
    score_breakdown: { accuracy: 0.92, efficiency: 0.85, completeness: 0.87 },
  },
  "tr-m3n4o5p6": {
    id: "tr-m3n4o5p6",
    task: "Investigate memory leak in stream processing pipeline",
    steps: [
      {
        index: 0,
        thought: "Let me check the stream processing service metrics and recent memory usage patterns.",
        action: { id: "check_metrics", name: "execute_command", arguments: { command: "curl -s localhost:9090/api/v1/query?query=process_resident_memory_bytes{job=~'stream-processor'}" } },
        observation: "Memory usage: started at 512MB, grew to 3.2GB over 4 hours. Pattern shows linear growth at ~13MB/min after each batch processing cycle.",
        latency_ms: 1500,
        started_at: Date.now() - 259200000,
        context_window: { used: 3800, limit: 128000 },
        token_prompt: 1700,
        token_completion: 280,
      },
      {
        index: 1,
        thought: "Linear memory growth suggests an object leak per batch. Let me get a heap profile to identify the leaking objects.",
        action: { id: "heap_profile", name: "execute_command", arguments: { command: "curl -s localhost:8000/debug/pprof/heap?debug=2 | head -200" } },
        observation: "Top allocations: 1) stream.Event (42% of heap, 1.8GB), 2) processor.Batch (28%, 1.1GB), 3) kafka.Message (12%, 480MB). Event objects are not being GC'd.",
        latency_ms: 2500,
        started_at: Date.now() - 259100000,
        context_window: { used: 7200, limit: 128000 },
        token_prompt: 2100,
        token_completion: 350,
      },
      {
        index: 2,
        thought: "Event objects are accumulating. Let me check the event processing pipeline — likely a goroutine leak where completed events aren't being released.",
        action: { id: "review_pipeline", name: "read_file", arguments: { path: "services/stream/pipeline.go", lines: 200 } },
        observation: "Found issue: Line 89 creates a new Event object per message, but Line 145's error path doesn't call event.Release(). Error conditions cause ~15% of events to leak.",
        latency_ms: 600,
        started_at: Date.now() - 259000000,
        context_window: { used: 10500, limit: 128000 },
        token_prompt: 800,
        token_completion: 180,
      },
      {
        index: 3,
        thought: "I can see the missing release call. However, this is a production code fix that needs a proper review process. Let me document it clearly.",
        action: { id: "document_leak", name: "write_file", arguments: { path: "fixes/stream_memory_leak.md", content: "Root Cause: event.Release() not called in error path at pipeline.go:145\n\nAffected: All stream processor instances\nImpact: OOM after ~8 hours of operation\nFix: Add deferred event.Release() after event creation on line 89\nPriority: P1" } },
        observation: "Findings documented. Referencing exact file and line for engineering fix.",
        latency_ms: 400,
        started_at: Date.now() - 258800000,
        context_window: { used: 12800, limit: 128000 },
        token_prompt: 500,
        token_completion: 150,
      },
      {
        index: 4,
        thought: "Task completed with identified root cause but no automated fix applied (requires PR review for production code changes).",
        action: null,
        observation: "Memory leak identified: missing event.Release() in pipeline.go:145 error path. Heap dump confirms Event objects are 42% of heap. Resolution requires code change in production repo.",
        latency_ms: 200,
        started_at: Date.now() - 258700000,
        context_window: { used: 14200, limit: 128000 },
        token_prompt: 300,
        token_completion: 120,
      },
    ],
    status: "failed",
    created_at: new Date(Date.now() - 259_200_000).toISOString(),
    total_tokens: 7800,
    context_window_peak: 14200,
    score: 0.32,
    score_breakdown: { accuracy: 0.50, efficiency: 0.30, completeness: 0.25 },
  },
  "tr-q7r8s9t0": {
    id: "tr-q7r8s9t0",
    task: "Audit dependency security vulnerabilities for Q3 release",
    steps: [
      {
        index: 0,
        thought: "Starting dependency audit. Let me check the package.json and requirements.txt files first.",
        action: { id: "read_deps", name: "read_file", arguments: { path: "package.json", lines: 100 } },
        observation: "Found 142 npm dependencies. Project uses React 18, Express 4.18, and several legacy packages.",
        latency_ms: 400,
        started_at: Date.now() - 600000,
        context_window: { used: 2800, limit: 128000 },
        token_prompt: 1200,
        token_completion: 180,
      },
      {
        index: 1,
        thought: "Let me check for known vulnerabilities by scanning the lockfile.",
        action: { id: "vuln_scan", name: "execute_command", arguments: { command: "npm audit --json 2>/dev/null | jq '{critical, high, moderate, low}'" } },
        observation: "Critical: 3, High: 7, Moderate: 12, Low: 5. Critical vulnerabilities: 1) express 4.18.0 RCE via send parameter, 2) lodash prototype pollution, 3) axios SSRF.",
        latency_ms: 3200,
        started_at: Date.now() - 599000,
        context_window: { used: 6200, limit: 128000 },
        token_prompt: 2200,
        token_completion: 350,
      },
      {
        index: 2,
        thought: "Critical findings. Let me check if the Python backend has similar issues.",
        action: { id: "pip_audit", name: "execute_command", arguments: { command: "pip-audit --requirement requirements.txt --format json 2>/dev/null" } },
        observation: "Python deps: 3 critical (Django < 5.0.3 SQL injection, urllib3 HTTP redirect, cryptography < 41.0.0). 4 high severity.",
        latency_ms: 2800,
        started_at: Date.now() - 596000,
        context_window: { used: 9800, limit: 128000 },
        token_prompt: 1800,
        token_completion: 300,
      },
      {
        index: 3,
        thought: "Let me also check the SDK dependencies and Docker base images.",
        action: { id: "check_sdk", name: "read_file", arguments: { path: "sdk/package.json", lines: 50 } },
        observation: "SDK uses axios 0.21.1 (vulnerable). Docker images: base node:18-alpine has 2 high severity CVEs (openssl, apk-tools).",
        latency_ms: 500,
        started_at: Date.now() - 593000,
        context_window: { used: 12000, limit: 128000 },
        token_prompt: 700,
        token_completion: 160,
      },
    ],
    status: "running",
    created_at: new Date(Date.now() - 600_000).toISOString(),
    total_tokens: null,
    context_window_peak: null,
    score: null,
    score_breakdown: null,
  },
  "tr-u1v2w3x4": {
    id: "tr-u1v2w3x4",
    task: "Optimize Redis cache invalidation strategy for user sessions",
    steps: [
      {
        index: 0,
        thought: "Let me examine the current session cache configuration and invalidation patterns.",
        action: { id: "check_config", name: "execute_command", arguments: { command: "redis-cli CONFIG GET 'maxmemory*'; redis-cli INFO stats | grep 'evicted_keys'" } },
        observation: "maxmemory: 4GB, maxmemory-policy: allkeys-lru, evicted_keys: 142380 in last 24h. High eviction rate indicates cache thrashing.",
        latency_ms: 1200,
        started_at: Date.now() - 345600000,
        context_window: { used: 3500, limit: 128000 },
        token_prompt: 1500,
        token_completion: 300,
      },
      {
        index: 1,
        thought: "142k evictions/day is very high. LRU with allkeys means active sessions are getting evicted. Let me check the session TTL settings.",
        action: { id: "check_ttl", name: "execute_command", arguments: { command: "redis-cli --scan --pattern 'session:*' | head -100 | xargs -I{} redis-cli TTL {} | sort -n | uniq -c | sort -rn | head -10" } },
        observation: "TTL distribution: 40% of sessions have TTL=86400 (24h), 30% have TTL=43200 (12h), 20% have no TTL, 10% have TTL=3600. 20% with no TTL will never expire.",
        latency_ms: 3200,
        started_at: Date.now() - 345500000,
        context_window: { used: 7800, limit: 128000 },
        token_prompt: 2000,
        token_completion: 280,
      },
      {
        index: 2,
        thought: "Found the problem: 20% of session keys have no TTL set, so they live forever until evicted by LRU. This causes active sessions to be evicted too. Let me trace where sessions are created.",
        action: { id: "trace_creation", name: "execute_command", arguments: { command: "grep -rn 'session' src/auth/*.ts | grep -E '(set|setex|expire)' | head -20" } },
        observation: "auth/session.ts:45 uses redis.set() without EXPIRE. auth/legacy.ts:89 uses setex with 3600s TTL. The legacy module sets TTL correctly, the new session module doesn't.",
        latency_ms: 1500,
        started_at: Date.now() - 345200000,
        context_window: { used: 11200, limit: 128000 },
        token_prompt: 900,
        token_completion: 210,
      },
      {
        index: 3,
        thought: "Root cause identified. The new session module (auth/session.ts) is missing TTL on session creation. Let me also check if there's a better eviction policy for this use case.",
        action: { id: "analyze_policy", name: "write_file", arguments: { path: "fixes/session_cache_analysis.md", content: "Issues:\n1. auth/session.ts missing EXPIRE on session keys → 20% immortal keys\n2. Eviction policy 'allkeys-lru' evicts active sessions under memory pressure\n3. 142k daily evictions causing user session drops\n\nFix:\na. Add EXPIRE 3600 to session.set() in auth/session.ts\nb. Change eviction policy to 'volatile-ttl' → evict only keys WITH TTL\nc. Set maxmemory to 8GB" } },
        observation: "Analysis document ready for handoff to the auth team.",
        latency_ms: 400,
        started_at: Date.now() - 345000000,
        context_window: { used: 14200, limit: 128000 },
        token_prompt: 600,
        token_completion: 180,
      },
      {
        index: 4,
        thought: "Let me also propose a quick win: adding a cache warming script for top user sessions on service restart.",
        action: { id: "warmup_script", name: "write_file", arguments: { path: "scripts/warm-sessions.sh", content: "#!/bin/bash\n# Warm top 1000 user sessions on service start\nredis-cli --scan --pattern 'session:*' | sort | head -1000 | while read key; do\n  redis-cli EXPIRE \"$key\" 3600\ndone" } },
        observation: "Quick-fix script created to retroactively set TTL on existing session keys.",
        latency_ms: 300,
        started_at: Date.now() - 344800000,
        context_window: { used: 15800, limit: 128000 },
        token_prompt: 500,
        token_completion: 140,
      },
      {
        index: 5,
        thought: "Let me verify the proposed fix by testing the eviction simulation.",
        action: { id: "simulate", name: "execute_command", arguments: { command: "redis-cli --eval scripts/estimate_savings.lua , session 1000 3600" } },
        observation: "With volatile-ttl + 1h TTL on sessions: estimated eviction reduction from 142k/day to ~2k/day. Active session drops reduced by 98%.",
        latency_ms: 1800,
        started_at: Date.now() - 344600000,
        context_window: { used: 18200, limit: 128000 },
        token_prompt: 800,
        token_completion: 200,
      },
      {
        index: 6,
        thought: "Solution validated. The fix requires minimal code change (one line: add EXPIRE) plus a config change (eviction policy). Estimated implementation: 30 minutes.",
        action: null,
        observation: "Cache optimization complete. 3 recommendations: 1) Add EXPIRE 3600 in auth/session.ts, 2) Switch eviction policy to volatile-ttl, 3) Quick retroactive TTL fix via warmup script. Estimated 98% reduction in session evictions.",
        latency_ms: 200,
        started_at: Date.now() - 344500000,
        context_window: { used: 19800, limit: 128000 },
        token_prompt: 400,
        token_completion: 190,
      },
    ],
    status: "success",
    created_at: new Date(Date.now() - 345_600_000).toISOString(),
    total_tokens: 13200,
    context_window_peak: 19800,
    score: 0.79,
    score_breakdown: { accuracy: 0.85, efficiency: 0.72, completeness: 0.80 },
  },
  "tr-y5z6a7b8": {
    id: "tr-y5z6a7b8",
    task: "Generate unit test suite for the billing module",
    steps: Array.from({ length: 12 }, (_, i) => ({
      index: i,
      thought: [
        "Starting with the billing module's core invoice generation logic.",
        "Reading the InvoiceService class to understand the public API surface.",
        "Writing the first test suite for invoice creation with valid data.",
        "Adding edge case tests for invoice with zero amounts and refunds.",
        "Now testing the payment processing pipeline with different payment methods.",
        "Writing tests for the subscription billing cycle logic.",
        "Adding tests for proration calculations during plan changes.",
        "Testing the tax calculation module with various jurisdictions.",
        "Adding discount and coupon application tests.",
        "Writing integration tests for the complete billing flow.",
        "Running the full test suite and fixing assertion issues.",
        "All tests pass. Generating coverage report.",
      ][i] ?? "Continuing test generation...",
      action: i < 11 ? { id: "write_test", name: "write_file", arguments: { path: `tests/billing/module_${i}.test.ts`, line_count: 60 + i * 5 } } : null,
      observation: [
        "Found 3 service classes, 8 public methods, 4 interfaces to test.",
        "InvoiceService: create(), void(), getPdf(), listByCustomer(). PaymentService: process(), refund(). SubscriptionService: cycle(), prorate().",
        "Test suite for create(): 6 test cases — happy path, missing fields, expired customer, duplicate prevention, currency validation, amount limits. All pass.",
        "Edge case tests: zero-amount invoice allowed for adjustments, refunds require original invoice reference, partial refunds supported.",
        "Payment pipeline: credit card (mock), PayPal, wire transfer. 9 test cases covering success, decline, timeout, and 3DS verification flows.",
        "Subscription billing: 8 test cases — monthly/quarterly/annual cycles, auto-renewal, cancellation at period end, immediate cancellation, grace period handling.",
        "Proration: 6 scenarios — upgrade mid-cycle (credit), downgrade (refund), add-on addition, add-on removal, pause, resume. Math validated against expected values.",
        "Tax calculation: 4 jurisdictions (US, EU, UK, JP). VAT, GST, state sales tax. 12 test cases including tax-exempt customers and cross-border scenarios.",
        "Discount/Coupon: percentage discounts, fixed amount, free trial period, stackable vs non-stackable, expired coupon handling, max usage limits. 8 test cases.",
        "Integration tests: 5 end-to-end flows — new customer → subscribe → generate invoice → process payment → send receipt. Mock external tax and payment APIs.",
        "Ran 54 tests: 54 passed. Coverage: 92% lines, 88% branches, 96% functions. Test duration: 1.2s.",
        "Test suite committed. Coverage report: 92% line coverage. All edge cases covered. CI pipeline updated to run billing tests on PR.",
      ][i] ?? "Processing...",
      latency_ms: [800, 600, 2500, 1800, 3200, 2800, 2400, 3500, 2100, 4200, 1200, 300][i] ?? 1000,
      started_at: Date.now() - 518400000 + i * 60000,
      context_window: { used: (i + 1) * 2500, limit: 128000 },
      token_prompt: i % 2 === 0 ? 900 + i * 100 : null,
      token_completion: i % 2 === 1 ? 200 + i * 20 : null,
    })),
    status: "success",
    created_at: new Date(Date.now() - 518_400_000).toISOString(),
    total_tokens: 22500,
    context_window_peak: 30000,
    score: 0.95,
    score_breakdown: { accuracy: 0.98, efficiency: 0.91, completeness: 0.96 },
  },
  "tr-c9d0e1f2": {
    id: "tr-c9d0e1f2",
    task: "Trace cross-service transaction failures in the order flow",
    steps: [
      {
        index: 0,
        thought: "Let me check the distributed tracing dashboard for failed transactions in the order flow.",
        action: { id: "traces", name: "execute_command", arguments: { command: "curl -s 'http://jaeger:16686/api/traces?service=order-api&limit=20&lookback=1h&tags=%7B%22error%22%3A%22true%22%7D' | jq '.data[].spans | length'" } },
        observation: "Found 34 error traces in the last hour. Average trace duration: 12.4s for failed transactions vs 0.8s for successful ones.",
        latency_ms: 1800,
        started_at: Date.now() - 604800000,
        context_window: { used: 4200, limit: 128000 },
        token_prompt: 1900,
        token_completion: 300,
      },
      {
        index: 1,
        thought: "The failed traces are 15x slower. Let me examine a representative trace to find the bottleneck.",
        action: { id: "trace_detail", name: "execute_command", arguments: { command: "curl -s 'http://jaeger:16686/api/traces?service=order-api&limit=1&lookback=1h&tags=%7B%22error%22%3A%22true%22%7D' | jq '.data[0].spans[] | {operationName, duration, status}'" } },
        observation: "Slowest span: 'inventory.update_stock' on 'inventory-service:50051' — duration: 8.2s, gRPC status: DEADLINE_EXCEEDED. Subsequent spans fail due to missing inventory lock.",
        latency_ms: 1200,
        started_at: Date.now() - 604700000,
        context_window: { used: 7200, limit: 128000 },
        token_prompt: 2100,
        token_completion: 280,
      },
      {
        index: 2,
        thought: "The inventory service gRPC call is timing out. Let me check the inventory service's current load and recent deployments.",
        action: { id: "inventory_check", name: "execute_command", arguments: { command: "kubectl describe deployment inventory-service; kubectl logs -n backend -l app=inventory-service --tail=50 | grep -E 'error|timeout|deadline'" } },
        observation: "Inventory service has 2 replicas, CPU at 95%. Pod logs show 'postgres: deadline exceeded' every 3-4 seconds on stock queries. Database has a queue of 47 waiting queries.",
        latency_ms: 2500,
        started_at: Date.now() - 604600000,
        context_window: { used: 11800, limit: 128000 },
        token_prompt: 1600,
        token_completion: 350,
      },
      {
        index: 3,
        thought: "The root cause is database contention in the inventory service. The stock query is locking rows and causing a chain reaction. Let me check the inventory DB schema and query patterns.",
        action: { id: "db_check", name: "execute_command", arguments: { command: "psql -d inventory -c \"SELECT pid, wait_event, query FROM pg_stat_activity WHERE state = 'active' AND wait_event IS NOT NULL;\"" } },
        observation: "4 connections in 'lock_timeout' state, all waiting on 'stock_reservation' row-level lock. Query pattern: UPDATE stock SET quantity = quantity - $1 WHERE product_id = $2 AND quantity >= $1. No index on product_id.",
        latency_ms: 1500,
        started_at: Date.now() - 604500000,
        context_window: { used: 15600, limit: 128000 },
        token_prompt: 1100,
        token_completion: 240,
      },
      {
        index: 4,
        thought: "Classic lock contention. The UPDATE statement locks the row for the entire transaction duration, and without an index, it's doing a sequential scan. Let me recommend the fix.",
        action: { id: "recommend", name: "write_file", arguments: { path: "fixes/inventory_deadlock.md", content: "Root Cause: Row-level lock contention on stock_reservation\n\nTrigger: UPDATE with sequential scan (missing index)\nImpact: 34 failed order transactions/hour\n\nFixes:\n1. CREATE INDEX idx_stock_product ON stock (product_id)\n2. Add SKIP LOCKED to the UPDATE to avoid blocking\n3. Increase inventory-service replicas from 2 to 4\n4. Add retry logic in order-api with exponential backoff" } },
        observation: "Analysis complete. 4 recommendations to resolve the cross-service transaction failures.",
        latency_ms: 400,
        started_at: Date.now() - 604400000,
        context_window: { used: 18400, limit: 128000 },
        token_prompt: 600,
        token_completion: 170,
      },
      {
        index: 5,
        thought: "Let me verify the index theory by checking if there are existing indexes on the stock table.",
        action: { id: "verify_index", name: "execute_command", arguments: { command: "psql -d inventory -c \"SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'stock';\"" } },
        observation: "Only PRIMARY KEY index on (id). No index on product_id. Adding the index will reduce the UPDATE from Seq Scan (cost=45000) to Index Scan (cost=0.5), eliminating the lock duration issue.",
        latency_ms: 600,
        started_at: Date.now() - 604300000,
        context_window: { used: 20400, limit: 128000 },
        token_prompt: 500,
        token_completion: 150,
      },
      {
        index: 6,
        thought: "Let me examine the topologically related traces to see if other services are affected by this inventory bottleneck.",
        action: { id: "check_downstream", name: "execute_command", arguments: { command: "curl -s 'http://jaeger:16686/api/dependencies?service=order-api&endTs=604800000' | jq '.'" } },
        observation: "Dependency graph shows cascading failures: order-api → inventory-service (8.2s timeout) → notification-service also affected (1.2s extra). No other downstream services impacted.",
        latency_ms: 1000,
        started_at: Date.now() - 604200000,
        context_window: { used: 23200, limit: 128000 },
        token_prompt: 800,
        token_completion: 190,
      },
      {
        index: 8,
        thought: "Good. The fix is well-scoped. Adding a single index and using SKIP LOCKED will resolve the issue. Let me also check if there's a recent change that triggered this degradation.",
        action: { id: "git_check", name: "execute_command", arguments: { command: "cd /repo && git log --oneline --since='48 hours ago' --all | head -10" } },
        observation: "Most recent merge: 'feat: real-time stock sync with warehouse system' (2 commits). This introduced the UPDATE pattern that's causing the lock contention.",
        latency_ms: 1800,
        started_at: Date.now() - 604000000,
        context_window: { used: 25000, limit: 128000 },
        token_prompt: 700,
        token_completion: 160,
      },
      {
        index: 9,
        thought: "Full picture: A recent feature merge introduced a new UPDATE pattern without proper indexing. Combined with the row-level locking, this causes cascading failures under load. The fix is straightforward.",
        action: null,
        observation: "Trace complete. Root cause: recent 'real-time stock sync' feature introduced UPDATE lock contention in inventory service due to missing index. 34 failed transactions/hour. Fix: add product_id index + SKIP LOCKED.",
        latency_ms: 200,
        started_at: Date.now() - 603900000,
        context_window: { used: 26800, limit: 128000 },
        token_prompt: 400,
        token_completion: 200,
      },
    ],
    status: "success",
    created_at: new Date(Date.now() - 604_800_000).toISOString(),
    total_tokens: 19500,
    context_window_peak: 26800,
    score: 0.71,
    score_breakdown: { accuracy: 0.78, efficiency: 0.65, completeness: 0.70 },
  },
};

/* ─── Tools ────────────────────────────────────────────────────── */

export const mockTools: ToolInfo[] = [
  {
    name: "read_file",
    description: "Read contents of a file with optional line range and line count limits.",
    parameters: {
      path: { type: "string", description: "Path to the file to read" },
      lines: { type: "number", description: "Number of lines to read (default: 200)", optional: true },
      start_line: { type: "number", description: "Starting line number (1-indexed)", optional: true },
      end_line: { type: "number", description: "Ending line number", optional: true },
    },
    enabled: true,
  },
  {
    name: "write_file",
    description: "Create or overwrite a file with the provided content. Creates intermediate directories if needed.",
    parameters: {
      path: { type: "string", description: "Path where to write the file" },
      content: { type: "string", description: "Content to write" },
    },
    enabled: true,
  },
  {
    name: "execute_command",
    description: "Run a shell command and capture its stdout and stderr output. Blocking with a 30s timeout.",
    parameters: {
      command: { type: "string", description: "Shell command to execute" },
      timeout: { type: "number", description: "Timeout in seconds (default: 30)", optional: true },
    },
    enabled: true,
  },
  {
    name: "edit_file",
    description: "Apply targeted text replacement in an existing file. Uses exact string matching for safety.",
    parameters: {
      path: { type: "string", description: "Path to the file to edit" },
      search: { type: "string", description: "Exact string to search for" },
      replace: { type: "string", description: "Replacement text" },
    },
    enabled: true,
  },
  {
    name: "read_logs",
    description: "Fetch log entries from the centralized logging system with filtering by service, level, and time range.",
    parameters: {
      service: { type: "string", description: "Service name filter" },
      level: { type: "string", description: "Log level filter (error, warn, info, debug)", optional: true },
      since: { type: "string", description: "ISO timestamp or relative time (e.g., '4h ago')", optional: true },
      lines: { type: "number", description: "Max lines to return", optional: true },
    },
    enabled: true,
  },
  {
    name: "generate_code",
    description: "Generate production-ready code based on a specification. Supports TypeScript, Python, Go, and Rust.",
    parameters: {
      language: { type: "string", description: "Target programming language" },
      specification: { type: "string", description: "Functional specification to implement" },
      include_tests: { type: "boolean", description: "Whether to include unit tests", optional: true },
    },
    enabled: true,
  },
  {
    name: "search_web",
    description: "Search the web for documentation, known issues, and best practices. Returns summarized snippets.",
    parameters: {
      query: { type: "string", description: "Search query" },
      sources: { type: "array", description: "Source types to search (docs, github, stackoverflow, general)", items: { type: "string" }, optional: true },
    },
    enabled: false,
  },
  {
    name: "analyze_code",
    description: "Static analysis of code for bugs, security issues, and optimization opportunities using AST-based analysis.",
    parameters: {
      path: { type: "string", description: "File or directory path to analyze" },
      depth: { type: "string", description: "Analysis depth (basic, full, security)", optional: true },
    },
    enabled: true,
  },
];

/* ─── Benchmark Tasks ─────────────────────────────────────────── */

export const mockBenchmarks: BenchmarkTask[] = [
  {
    name: "code_review",
    task: "Review a pull request containing a new authentication middleware implementation. Identify security flaws, performance issues, and code quality problems.",
    description: "Evaluate PR review quality and thoroughness",
  },
  {
    name: "debugging",
    task: "A production service is returning 502 errors intermittently. Investigate the attached logs and configuration to identify the root cause and propose a fix.",
    description: "Debug production incidents from logs and config",
  },
  {
    name: "data_analysis",
    task: "Analyze the attached CSV dataset of user sessions (10k rows). Identify patterns in user churn, propose 3 actionable recommendations, and generate summary statistics.",
    description: "Analyze data and generate business insights",
  },
  {
    name: "optimization",
    task: "The attached SQL query runs in 45 seconds. Optimize it to run under 500ms without changing the output. Include the optimized query and indexing strategy.",
    description: "Optimize slow database queries",
  },
];

/* ─── Compare Data ────────────────────────────────────────────── */

export const mockCompareResponse: CompareResponse = {
  trajectories: [
    {
      id: "tr-a1b2c3d4",
      task: "Analyze production logs and isolate the latency spike in the payment service",
      status: "success",
      created_at: new Date(Date.now() - 3_600_000).toISOString(),
      total_steps: 8,
      total_latency_ms: 10500,
    },
    {
      id: "tr-i9j0k1l2",
      task: "Refactor legacy SQL queries for the reporting dashboard",
      status: "success",
      created_at: new Date(Date.now() - 172_800_000).toISOString(),
      total_steps: 10,
      total_latency_ms: 25900,
    },
    {
      id: "tr-u1v2w3x4",
      task: "Optimize Redis cache invalidation strategy for user sessions",
      status: "success",
      created_at: new Date(Date.now() - 345_600_000).toISOString(),
      total_steps: 7,
      total_latency_ms: 11300,
    },
  ],
  aligned_steps: [
    {
      step_index: 0,
      trajectories: [
        { index: 0, thought: "I'll start by fetching the recent production logs...", action: { id: "read_logs", name: "read_file", arguments: { path: "/var/log/payment-service/error.log", lines: 500 } }, observation: "Returned 487 lines. 23 ERROR entries...", latency_ms: 1200 },
        { index: 0, thought: "Let me start by reviewing the current reporting dashboard queries...", action: { id: "read_queries", name: "read_file", arguments: { path: "dashboard/reports/queries.sql", lines: 300 } }, observation: "Found 15 legacy queries. 8 use SELECT *...", latency_ms: 1200 },
        { index: 0, thought: "Let me examine the current session cache configuration...", action: { id: "check_config", name: "execute_command", arguments: { command: "redis-cli CONFIG GET 'maxmemory*'" } }, observation: "maxmemory: 4GB, evicted_keys: 142380...", latency_ms: 1200 },
      ],
      tools_differ: true,
      tool_names: ["read_file", "read_file", "execute_command"],
    },
    {
      step_index: 1,
      trajectories: [
        { index: 1, thought: "The p95 latency is 4x above threshold. Checking DB query times...", action: { id: "run_query", name: "execute_command", arguments: { command: "psql -c \"SELECT queryid, mean_time_ms...\"" } }, observation: "Top query: 287ms mean. Missing index on created_at.", latency_ms: 3400 },
        { index: 1, thought: "Biggest offender is revenue query doing full scan. Checking execution plan...", action: { id: "explain_query", name: "execute_command", arguments: { command: "EXPLAIN ANALYZE SELECT..." } }, observation: "Seq Scan on orders. Actual time: 6380ms.", latency_ms: 4500 },
        { index: 1, thought: "142k evictions/day is very high. Checking session TTL settings...", action: { id: "check_ttl", name: "execute_command", arguments: { command: "redis-cli --scan --pattern 'session:*'" } }, observation: "TTL distribution: 40% at 24h, 20% no TTL.", latency_ms: 3200 },
      ],
      tools_differ: false,
      tool_names: ["execute_command", "execute_command", "execute_command"],
    },
    {
      step_index: 2,
      trajectories: [
        { index: 2, thought: "Found root cause: missing composite index. Also checking Redis cache...", action: { id: "check_cache", name: "execute_command", arguments: { command: "redis-cli --scan --pattern 'payment:*'" } }, observation: "Cache miss rate: 68%", latency_ms: 800 },
        { index: 2, thought: "Missing index on orders.created_at. Creating it...", action: { id: "create_index", name: "execute_command", arguments: { command: "CREATE INDEX CONCURRENTLY..." } }, observation: "Index created successfully.", latency_ms: 3200 },
        { index: 2, thought: "20% of session keys have no TTL. Tracing session creation code...", action: { id: "trace_creation", name: "execute_command", arguments: { command: "grep -rn 'session' src/auth/*.ts" } }, observation: "auth/session.ts uses set() without EXPIRE.", latency_ms: 1500 },
      ],
      tools_differ: true,
      tool_names: ["execute_command", "execute_command", "execute_command"],
    },
    {
      step_index: 3,
      trajectories: [
        { index: 3, thought: "Verifying index recommendation by checking schema...", action: { id: "get_schema", name: "read_file", arguments: { path: "migrations/2024_schema.sql", lines: 200 } }, observation: "Missing composite index confirmed.", latency_ms: 600 },
        { index: 3, thought: "Now rewriting queries to be efficient. Starting with revenue report...", action: { id: "rewrite_query", name: "write_file", arguments: { path: "dashboard/reports/queries_optimized.sql" } }, observation: "Optimized query written.", latency_ms: 600 },
        { index: 3, thought: "Analyzing session eviction and proposing fix strategy...", action: { id: "analyze_policy", name: "write_file", arguments: { path: "fixes/session_cache_analysis.md" } }, observation: "Analysis documented.", latency_ms: 400 },
      ],
      tools_differ: true,
      tool_names: ["read_file", "write_file", "write_file"],
    },
    {
      step_index: 4,
      trajectories: [
        { index: 4, thought: "Checking recent deployments that may have caused regression...", action: { id: "check_deploy", name: "execute_command", arguments: { command: "kubectl get deployments -n payment" } }, observation: "Last deployment 6h ago changed connection pool settings.", latency_ms: 1500 },
        { index: 4, thought: "Verifying optimization — running EXPLAIN ANALYZE on rewritten query...", action: { id: "verify", name: "execute_command", arguments: { command: "EXPLAIN ANALYZE optimized query..." } }, observation: "142ms vs 6380ms. 44x improvement.", latency_ms: 3800 },
        { index: 4, thought: "Creating warmup script for retroactive TTL fix...", action: { id: "warmup_script", name: "write_file", arguments: { path: "scripts/warm-sessions.sh" } }, observation: "Quick-fix script created.", latency_ms: 300 },
      ],
      tools_differ: false,
      tool_names: ["execute_command", "execute_command", "write_file"],
    },
  ],
  max_steps: 5,
};

/* ─── Failure Analysis ─────────────────────────────────────────── */

export const mockFailureSummary: FailureSummary = {
  planning: 35,
  execution: 28,
  context: 22,
  budget: 15,
};

export const mockFailureReports: Record<string, FailureReport> = {
  "tr-e5f6g7h8": {
    dimensions: { planning: 25, execution: 60, context: 10, budget: 5 },
    dominant: "execution",
    evidence: [
      {
        dimension: "execution",
        step_index: 3,
        reason: "Goroutine stack trace analysis identified blocking mutex lock, but the agent did not propose a code fix — only documented the issue",
        severity: 0.85,
        details: { file: "services/checkout/checkout.go", line: 287, pattern: "sync.Mutex.Lock without timeout" },
      },
      {
        dimension: "execution",
        step_index: 1,
        reason: "Failed to query the service directly to verify upstream health before tracing through nginx config, wasting 2 steps on indirection",
        severity: 0.60,
        details: { suggested: "Direct service health check would have identified the issue 3 steps earlier" },
      },
      {
        dimension: "planning",
        step_index: 0,
        reason: "Bypassed the monitoring dashboard and alert history, starting from raw logs without understanding the broader context",
        severity: 0.45,
        details: { missed_sources: ["Grafana dashboard", "PagerDuty alert history", "Datadog APM traces"] },
      },
      {
        dimension: "context",
        step_index: 4,
        reason: "Did not check whether a rolling restart or fallback mechanism exists as a temporary mitigation while developing the permanent fix",
        severity: 0.30,
        details: null,
      },
    ],
    needs_human_review: true,
  },
  "tr-m3n4o5p6": {
    dimensions: { planning: 40, execution: 30, context: 20, budget: 10 },
    dominant: "planning",
    evidence: [
      {
        dimension: "planning",
        step_index: 0,
        reason: "Started with raw metrics endpoint instead of checking the existing monitoring dashboard with pre-configured memory usage panels",
        severity: 0.70,
        details: { suggestion: "Grafana dashboard 'Stream Processor Overview' has memory trend charts that would show the leak pattern immediately" },
      },
      {
        dimension: "execution",
        step_index: 2,
        reason: "Correctly identified the leaking Event objects but did not estimate the OOM timeline or recommend a temporary mitigation (e.g., increasing memory limit)",
        severity: 0.55,
        details: null,
      },
    ],
    needs_human_review: true,
  },
};

/* ─── Policies ─────────────────────────────────────────────────── */

export const mockPolicies: PolicyVersion[] = [
  {
    version_id: "pol-v1",
    version_display: "v1.0.0",
    parent_version: null,
    patch: {
      system_prompt_suffix: "\n\nAlways verify your assumptions with data before drawing conclusions.",
    },
    rationale: "Initial policy baseline — basic fact-checking discipline",
    expected_impact: { score_change: "neutral", reliability: "increased" },
    confidence: "high",
    status: "active",
    score_delta: 0.0,
    reject_reason: null,
    created_at: new Date(Date.now() - 1_000_000_000).toISOString(),
  },
  {
    version_id: "pol-v2",
    version_display: "v1.1.0",
    parent_version: "pol-v1",
    patch: {
      system_prompt_suffix: "\n\nAlways verify your assumptions with data before drawing conclusions.\nWhen investigating production issues, start with the monitoring dashboard and alert history before diving into raw logs.",
      tool_priority_bias: { read_logs: 0.8, execute_command: 1.2 },
    },
    rationale: "Improve debugging efficiency by prioritizing monitoring context",
    expected_impact: { time_to_root_cause: "-30% estimated" },
    confidence: "medium",
    status: "pending_review",
    score_delta: 0.12,
    reject_reason: null,
    created_at: new Date(Date.now() - 500_000_000).toISOString(),
  },
  {
    version_id: "pol-v3",
    version_display: "v1.2.0",
    parent_version: "pol-v1",
    patch: {
      system_prompt_suffix: "\n\nAlways verify your assumptions with data before drawing conclusions.\nAfter identifying a root cause, always propose a concrete remediation plan with implementation steps.",
      max_steps_override: 20,
    },
    rationale: "Push for actionable remediation instead of analysis-only outputs",
    expected_impact: { fix_rate: "+40% estimated" },
    confidence: "low",
    status: "pending_review",
    score_delta: 0.05,
    reject_reason: null,
    created_at: new Date(Date.now() - 300_000_000).toISOString(),
  },
  {
    version_id: "pol-v4",
    version_display: "v1.0.1",
    parent_version: "pol-v1",
    patch: {
      system_prompt_suffix: "",
      context_strategy: "sliding_window",
    },
    rationale: "Stability release — revert to proven strategy with minor context optimization",
    expected_impact: { context_utilization: "+15%", token_waste: "-20%" },
    confidence: "high",
    status: "reverted",
    score_delta: -0.03,
    reject_reason: "Sliding window caused inconsistency in long-running analyses — rolled back in favor of explicit summarization",
    created_at: new Date(Date.now() - 700_000_000).toISOString(),
  },
  {
    version_id: "pol-v5",
    version_display: "v2.0.0",
    parent_version: "pol-v2",
    patch: {
      system_prompt_suffix: "\n\nAlways verify your assumptions with data before drawing conclusions.\nWhen investigating production issues, start with the monitoring dashboard and alert history.\nFor every finding, produce: (1) Root cause, (2) Evidence, (3) Recommended fix, (4) Estimated impact.",
      tool_priority_bias: { read_logs: 1.5, execute_command: 1.0, analyze_code: 1.3 },
      context_strategy: "summarization",
      max_steps_override: 25,
    },
    rationale: "Major overhaul: structured output format + prioritized evidence gathering + summarization for long contexts",
    expected_impact: { score: "+0.15 estimated", consistency: "highly improved", token_cost: "+18%" },
    confidence: "medium",
    status: "pending_review",
    score_delta: null,
    reject_reason: null,
    created_at: new Date(Date.now() - 50_000_000).toISOString(),
  },
];

export const mockActivePolicy: PolicyVersion = mockPolicies[0];

export const mockWarmupStatus: WarmupStatus = {
  total_trajectories: 18,
  threshold: 30,
  ready: false,
};
