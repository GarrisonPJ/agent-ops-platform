/**
 * Tailwind CSS v4 configuration.
 *
 * Design tokens are defined via `@theme` in `src/index.css`.
 * This file exists for tooling support (e.g. IDE autocomplete).
 * No v3-style theme overrides needed — v4 handles everything through CSS.
 */
import type { Config } from "tailwindcss";

export default {} satisfies Config;
